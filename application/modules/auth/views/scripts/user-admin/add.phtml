<?php $this->dojo()->onLoadCaptureStart();?>
function(){
	f = dijit.byId('newUserForm');
	s = dijit.byId('addNewUser');
	dojo.connect(s,'_onClick', function(e){
		if (!f.isValid()) {
			s.cancel();
			alert('ป้อนข้อมูลไม่ครบ กรุณาตรวจสอบอีกครั้ง');
		}
	});
}
<?$this->dojo()->onLoadCaptureEnd();?>

<h1>กรอกข้อมูลรายละเอียดผู้ใช้</h1>
<?=$this->newUserForm?>
